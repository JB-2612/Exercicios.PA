#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct Dados {
    char nome[10];
    int idade;
    char cidade[10];
};
FILE *p;

int main(){

    p = fopen("Tabela.csv", "w" );











    return 0;
}